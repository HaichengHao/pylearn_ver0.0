# editor hc
# date: 2023/2/21 8:19
def add(a,b):
    return a+b
if __name__ == '__main__':
    print(add(10,20))#只有当点击运行calc2时，才会执行运算，也就是说当calc2作为主程序时，才会执行
# 30 <--此为calc2.py作为主程序时运行了第6行代码，但也仅在其作为主程序时才会运行。